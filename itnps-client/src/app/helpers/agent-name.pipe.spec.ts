import { AgentNamePipe } from './agent-name.pipe';

describe('AgentNamePipe', () => {
  it('create an instance', () => {
    const pipe = new AgentNamePipe();
    expect(pipe).toBeTruthy();
  });
});
